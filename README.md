# nvimrc
This is my personal nvim configuration and a simple go script which should put everything where it should
I keep it super basic so it is probably most useful for myself

```bash
# After cloning this repository and all of it's submodules
go run tool.go install ~/.config/nvim/
```
